"use strict";

var g = (a) => { return a*2; }

var h = (a) => a * 2;

var arr = ['alma', 'körte', 'banán', 'barack', 'kiwi'];
arr.filter(item => item.length === 4);

var nums = [2, 4, 5, 6, 8, 11];
nums.reduce((x, y) => x + y, 0);

var address = {
    name: 'Levente',
    address: 'Budapest',
    phone: '0630-9400675'
}

